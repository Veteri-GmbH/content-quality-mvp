The user wants to replace the current basic `Home.tsx` with a modern dashboard-style start page.

Technical Plan:

1.  **Modify `ui/src/pages/Home.tsx`**:
    *   Replace the current implementation with a Dashboard layout.
    *   **Data Fetching**:
        *   Use `api.getAudits()` to fetch the list of audits.
        *   Use `useAuth()` to get the current user's name for a personalized greeting.
    *   **Layout**:
        *   Use a responsive grid layout (single column on mobile, two columns on desktop).
        *   **Section 1: Hero/Welcome**:
            *   Display "Welcome back, {user.displayName || user.email}".
            *   Brief subtitle.
        *   **Section 2: Quick Actions**:
            *   Primary `Button` linking to `/audits/new` ("Neues Audit starten").
            *   Secondary `Button` linking to `/audits` ("Alle Audits anzeigen").
        *   **Section 3: Key Metrics (Stats)**:
            *   Display simple metrics derived from the fetched audits:
                *   Total Audits (count).
                *   Completed Audits (status === 'completed').
                *   (Optional) Average Score (if available in the list, otherwise skip).
        *   **Section 4: Recent Activity**:
            *   Display a list of the 3-5 most recent audits.
            *   Each item should show:
                *   Date (formatted).
                *   Sitemap URL (truncated if needed).
                *   Status badge (using similar logic/colors as `AuditTable` or a new helper).
                *   Link to `/audits/{audit.id}`.
    *   **Components**:
        *   Use ShadCN `Card`, `CardHeader`, `CardTitle`, `CardContent`, `CardDescription` for containers.
        *   Use `Button` for actions.
        *   Use `Skeleton` for loading states.
        *   Use `lucide-react` icons (e.g., `Plus`, `List`, `Activity`, `CheckCircle`).

2.  **No new API endpoints required**: The existing `getAudits` endpoint returns the necessary data (`Audit` interface includes `created_at`, `status`, `sitemap_url`).

3.  **Styling**:
    *   Use Tailwind CSS classes for spacing, typography, and responsive design.
    *   Ensure dark mode compatibility (using standard ShadCN/Tailwind classes).

4.  **Verification**:
    *   Check if the user is authenticated (which is handled by `App.tsx` routing/guard).
    *   Handle loading and empty states gracefully (e.g., "No audits yet - start your first one!").

